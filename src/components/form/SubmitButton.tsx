import React from "react";

const SubmitButton = () => {
  return (
    <button
      className="w-full rounded-lg bg-blue-400 px-5 py-2 text-white  transition-all hover:bg-blue-700"
      type="submit"
    >
      Sign in
    </button>
  );
};

export default SubmitButton;
